@page "/Pages/SearchAndSelect/AccountSelect"

@*
  FLOW: Generic Selector (Account)
  SETS: selectedAccountNo, selectedAccountName
  READS: flowId, redirTo (query param)
*@

@functions { }

<!DOCTYPE html>
<html>
<head>
    <title>Select Account</title>
    <link rel="stylesheet" href="/Pages/static/u.css">
    <style>
        body {
            font-family: sans-serif;
            padding: 20px;
        }

        input {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
            margin-bottom: 10px;
        }

        .card {
            border: 1px solid #ccc;
            padding: 10px;
            margin-bottom: 5px;
            cursor: pointer;
            border-radius: 4px;
        }

            .card:hover {
                background: #f0f0f0;
            }

        .current-bar {
            background: #e3f2fd;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
            border: 1px solid #90caf9;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

            .current-bar button {
                padding: 5px 10px;
                cursor: pointer;
                background: #1976d2;
                color: white;
                border: none;
                border-radius: 3px;
            }
    </style>
</head>
<body>
    <h2>Select Account</h2>

    <!-- Current Selection Bar -->
    <div id="currentBar" style="display:none;" class="current-bar">
        <div>
            <strong>Current:</strong> <span id="currentName"></span> (<span id="currentId"></span>)
        </div>
        <button onclick="proceedWithCurrent()">Proceed</button>
    </div>

    <input type="text" id="query" placeholder="Search Account Name or No..." oninput="search()">
    <div id="results">Start typing to search...</div>

    <script src="/Pages/static/u.js"></script>
    <script>
        const params = new URLSearchParams(window.location.search);
        const flowId = params.get('flowId');
        // The URL to redirect to AFTER selection
        const redirTo = params.get('redirTo');

        function getCookie(n) { const v = `; ${document.cookie}`; const p = v.split(`; ${n}=`); return p.length === 2 ? p.pop().split(';').shift() : null; }

        async function init() {
            const token = getCookie("auth-token");

            // Check if already set
            const resId = await fetch('/GetFlowData', {
                method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                body: JSON.stringify({ FlowId: flowId, Key: "selectedAccountNo" })
            });
            const dataId = await resId.json();

            if(dataId.value) {
                const resName = await fetch('/GetFlowData', {
                    method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                    body: JSON.stringify({ FlowId: flowId, Key: "selectedAccountName" })
                });
                const dataName = await resName.json();

                document.getElementById('currentId').innerText = dataId.value;
                document.getElementById('currentName').innerText = dataName.value || "Unknown";
                document.getElementById('currentBar').style.display = 'flex';
            }
        }

        async function search() {
            const token = getCookie("auth-token");
            const q = document.getElementById('query').value;

            const res = await fetch('/SearchAccountsWeb', {
                method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                body: JSON.stringify({ Query: q })
            });

            const data = await res.json();
            const resultsDiv = document.getElementById('results');

            if (!data || data.length === 0) {
                resultsDiv.innerHTML = "<p>No accounts found.</p>";
                return;
            }

            let html = "";
            data.forEach(a => {
                html += s`<div class="card" onclick="select(${a.accountNo}, '${a.accountName.replace(/'/g, "\\'")}')">
                    <strong>${a.accountName}</strong> (ID: ${a.accountNo})<br>
                    <small>Type: ${a.accountType} | Bal: ${a.balance || 0}</small>
                </div>`;
            });
            resultsDiv.innerHTML = html;
        }

        async function select(id, name) {
            const token = getCookie("auth-token");

            await fetch('/SetFlowData', {
                method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                body: JSON.stringify({ FlowId: flowId, Key: "selectedAccountNo", Value: id.toString() })
            });
            await fetch('/SetFlowData', {
                method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                body: JSON.stringify({ FlowId: flowId, Key: "selectedAccountName", Value: name })
            });

            proceed();
        }

        async function proceedWithCurrent() {
            proceed();
        }

        function proceed() {
            if (redirTo) {
                // redirTo already contains flowId in this specific usage, but we check just in case
                // usually redirTo = "/Path/Page?flowId=..."
                window.location.href = redirTo;
            } else {
                alert("No redirect target specified.");
            }
        }

        init();
    </script>
</body>
</html>