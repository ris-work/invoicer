@page "/"
@using Microsoft.AspNetCore.Mvc

@functions {
    public IActionResult OnGet()
    {
        // Redirects to the Index page in the same folder
        return RedirectToPage("Index");
    }
}